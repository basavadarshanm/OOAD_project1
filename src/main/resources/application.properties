# Fallback defaults (env variables are preferred). Copy .env.example to .env and set DB_URL.
db.url=jdbc:h2:./data/online_banking;MODE=MySQL;AUTO_SERVER=TRUE;LOCK_TIMEOUT=5000
db.user=sa
db.password=
# Optional pool settings
pool.maximumPoolSize=10
pool.connectionTimeoutMs=30000
pool.idleTimeoutMs=600000
pool.maxLifetimeMs=1800000
